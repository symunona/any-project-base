# branding/ — AGENTS.md

Read first:
1. `/AGENTS.md` (root)
2. `architecture/branding.md` ← full branding strategy + apply-branding targets

---

## Purpose

Source of truth for project visual identity: logos, colors.
Edit here, propagate everywhere via `just setup apply-branding`.
Never edit propagated files directly — they will be overwritten.

## Directory Structure

```
branding/
  logo-large.svg        ← full logo: icon + wordmark
  logo-small.svg        ← icon only, for small sizes
  logo-favicon.svg      ← 32×32 optimized, no text
  colors.yaml           ← color source of truth (edit this, not globals.css)
  palette.js            ← GENERATED by agent during branding setup step
                        ← Source: setup/branding/branding.sh
                        ← Re-generate: just setup branding
  palette-template.html ← static preview page, loads palette.js — never generated
  README.md             ← human instructions for manual override
```

## colors.yaml

```yaml
# Edit this file directly to change colors.
# Run: just setup apply-branding to propagate changes everywhere.
primary:   "#6366f1"
secondary: "#8b5cf6"
accent:    "#06b6d4"
success:   "#10b981"
danger:    "#ef4444"
```

## Rules

- **Edit SVGs here** using any vector editor (Figma, Inkscape, Illustrator). Export as SVG, replace file, run `just setup apply-branding`.
- **Edit colors** in `colors.yaml` only. Never touch `commons/styles/globals.css` directly.
- **Never edit `palette.js`** — GENERATED. Re-generate via `just setup branding`.
- **Never edit propagated files** — `manifest.json`, `app.json` branding fields, `globals.css` are all overwritten by `apply-branding`.
- SVG files are owned by this directory. All other locations are copies.

## Propagation targets

`just setup apply-branding` updates:

| Source | Target |
|--------|--------|
| `colors.yaml` | `commons/styles/globals.css` — CSS custom properties |
| `colors.yaml` → primary | `client-portal/public/manifest.json` → theme_color |
| `colors.yaml` → primary | `admin-portal/public/manifest.json` → theme_color |
| `colors.yaml` | `mobile-app/app.json` → primaryColor, splash, navigationBarColor |
| `colors.yaml` | `project.yaml` → theme_colors block |
| `colors.yaml` | `supabase/migrations/…email_templates_v2.sql` → inline hex in body_html |
| `colors.yaml` | DB `public.email_templates.body_html` → live hex replacement (if Supabase running) |
| `logo-large.svg` | `client-portal/public/logo.svg` |
| `logo-large.svg` | `admin-portal/public/logo.svg` |
| `logo-large.svg` | `landing/assets/logo.svg` |
| `logo-small.svg` | `client-portal/public/logo-small.svg` |
| `logo-small.svg` | `admin-portal/public/logo-small.svg` |
| `logo-small.svg` | `mobile-app/assets/logo.svg` |
| `logo-favicon.svg` | `client-portal/public/favicon.svg` |
| `logo-favicon.svg` | `admin-portal/public/favicon.svg` |
| `logo-favicon.svg` | `landing/assets/favicon.svg` |

## When agent touches this directory

- Branding setup step: agent writes `palette.js` only
- Agent never modifies SVG files after initial generation
- If user requests logo changes: regenerate via `just setup branding` with custom instructions
- If user requests color changes: update `colors.yaml`, run `just setup apply-branding`

## Checker

`setup/checks/branding_check.sh`:
- `colors.yaml` exists and has all 5 required keys
- All propagated CSS vars match `colors.yaml` values
- All manifest `theme_color` values match `colors.yaml` primary
- All SVG destinations exist and are non-empty
- `palette.js` has GENERATED header
- `globals.css` has GENERATED header
